# Short names are supported; SMTP_*, EMAIL_*, and MAIL_* aliases are accepted too.
HOST=smtp.gmail.com
PORT=587
USERNAME=your_user
PASSWORD=your_app_password
FROM=from@example.com
TO=to@example.com
ENABLE_SSL=true
